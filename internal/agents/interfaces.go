package agents

import (
	"github.com/codeready/go-tarsy-bot/internal/shared"
)

// LLMIntegrationInterface is an alias to the shared interface
type LLMIntegrationInterface = shared.LLMIntegrationInterface

// EnhancedGenerateRequest is an alias to the shared type
type EnhancedGenerateRequest = shared.EnhancedGenerateRequest

// GenerateWithToolsRequest is an alias to the shared type
type GenerateWithToolsRequest = shared.GenerateWithToolsRequest

// GenerateRequest is an alias to the shared type
type GenerateRequest = shared.GenerateRequest

// Message is an alias to the shared type
type Message = shared.Message

// LLMResponse is an alias to the shared type
type LLMResponse = shared.LLMResponse